function start(str, start) {
    let index = str.indexOf(start);
    if(index >= 0){
        console.log("true");
    }
    else {
        console.log("false");

    }
}

start("244", "Marketing Fundamentals, s");